# Example NGINX server block for a Meralda site
server {
    listen 80;
    server_name yourdomain.com;
    root /path/to/yourproject/src/public_html;

    include /path/to/nginx-meralda-includes.conf;

    # ...other server config...
}
