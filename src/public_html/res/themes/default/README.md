# Default Theme — Assets

Static files (CSS) for the `default` theme.

- `css/custom.css` — Overrides on top of the standard UI2 CSS stack.

For the theme's PHP logic, see [`src/mwap/modules/themes/default/`](../../../../mwap/modules/themes/default/README.md).
